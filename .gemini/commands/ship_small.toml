description = "One-pass small task execution with scoped verification"
prompt = '''---
description: One-pass small task execution with scoped verification
argument-hint: [TASK]
model: sonnet
capability_class: implementer
---

# Ship Small

Load and execute the canonical command at `agent-commands/ship_small.md`.

Task:
$ARGUMENTS

If the command runner does not substitute arguments, use the text supplied by the user after the slash command as the task.
'''
