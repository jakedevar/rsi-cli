---
description: One-shot context, plan, implement, and verify with durable artifacts
model: opus
capability_class: architect
---

# Drive Plan

Load and execute the canonical command at `agent-commands/drive_plan.md`.

Task:
$ARGUMENTS

If the command runner does not substitute arguments, use the text supplied by the user after the slash command as the task.
